CREATE TABLE `burung` (
  `no` varchar(11) NOT NULL auto_increment,
  `noring` varchar(100) NOT NULL,
  `jenis` varchar(100) NOT NULL,
  `umur` varchar(3) NOT NULL,
  
  PRIMARY KEY  (`noring`)
);